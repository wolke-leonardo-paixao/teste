#!/bin/bash

# Obter o nome do branch atual
current_branch=$(git rev-parse --abbrev-ref HEAD)

# Executar o script SQL dentro do Docker, passando o nome do branch como uma variável
docker exec -i mysql mysql -u root -p123456 -e "SET @branch_name='$current_branch'; source src/script.sql"
